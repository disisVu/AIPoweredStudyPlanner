{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"],
  "include": [
    "src/**/*.ts",        // Include TypeScript files
    "src/templates/**/*"  // Include templates folder
  ]
}
